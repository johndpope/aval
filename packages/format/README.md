# @rendered-motion/format

Strict parser, validator, writer, AVC inspector, PNG decoder, and graph adapter
for compiled Rendered Motion wire format `0.1`. All hostile byte arithmetic,
layout, media profile, and digest gates are bounded.

See the repository format `0.1` reference.
