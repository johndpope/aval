# @rendered-motion/player-web

Web-only Rendered Motion runtime: bounded range/integrity loader, WebCodecs
scheduler and worker, WebGL packed-alpha presentation, strict static fallback,
and page-wide decoder/byte resource arbitration.

Most applications should prefer `@rendered-motion/element`. See the repository
performance and budget guide.
