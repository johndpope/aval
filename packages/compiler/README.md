# @rendered-motion/compiler

Authoring API and `rma` CLI for project schema `0.2` and compiled wire format
`0.1`. The compiler uses caller-installed FFmpeg/FFprobe and libx264; no native
codec tool is bundled or downloaded.

See the repository compiler guide.
